[project]
name = "image-generation"
version = "0.1.0"
description = "Generate images for exercises and foods using Nano Banana 2 via Hugging Face"
requires-python = ">=3.12"
dependencies = [
    "gradio-client>=2.3.0",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
    "websockets>=16.0",
]
